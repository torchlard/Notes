### matches
element.matches("li")
just like `$("li")` in jQuery

##
['1','2','3'].map(parseInt) === [1, NaN, NaN]
because parseInt need (val, radix)

## save sort
[].sort((a,b) => a-b)

























