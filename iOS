# pulling elements from storyboard is not only adding a lne of code in ViewController,
but some more

# Swift error : signal SIgABRT
there are some unconnected view 

# ImageView intrinsic size: only used in runtime
